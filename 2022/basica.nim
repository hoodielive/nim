let number = 1
let name = "String"
let decimal = 1.0
let booleanvar = false 

echo number

echo "The whole number is", number

var name_again: string = "Lawrence"

# Nim does have type inference.

var a: int = 1

# However.
var b = 7 # is permitted.

echo b, a

